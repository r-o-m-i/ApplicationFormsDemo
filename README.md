# ApplicationFormsDemo
Simple application forms demo that reads the user input into a HTML form and registers the users into mysql database.
used: HTML, jsp, css, hibernate, maven,core and advanced java.
